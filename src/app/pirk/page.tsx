import type { Metadata } from "next";
import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata: Metadata = { title: "Pirk" };

export default function Page() {
  return <PlaceholderPage title="Pirk" />;
}
